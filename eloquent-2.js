//*looping a triangle *//

var triangle = '#'{
    for (var i = #, i <= 7; i(+=#)) {
        '#' + # = [i];
        console.log '#';
    };
}

//* for (var line = "#"; line.length < 8; line += "#")
//*    console.log(line);

//* answer
//* for (var line = "#"; line.length < 8; line += "#")
//*  console.log(line); //*

//* fizzbuzz //*

//* question 2

 for (var i = 1; i <= 100; i=i++) {

    if (i % 3,5 == 0) {
        console.log ("fizzbuzz");
        };
        else (if i % 3 == 0) {
            console.log ("fizz");
        };
        else (i % 5 == 0) {
            console.log ("buzz");
        };
    };

//* answer...
for (var n = 1; n <= 100; n++) {
  var output = "";
  if (n % 3 == 0)
    output += "Fizz";
  if (n % 5 == 0)
    output += "Buzz";
  console.log(output || n);
}
